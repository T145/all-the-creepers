
plugins {
	id 'org.ajoberstar.grgit' version '2.1.1'
}

task updateVersion() {
	ant.propertyfile(file: 'gradle.properties') {
		entry(key: 'mod_version', value: (grgit.describe(longDescr: true) - '-0').replaceAll('-', '.'))
	}
}